{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-window-chrome",
  "description": "Allow the bundled macOS page to reveal native window controls.",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": ["allow-set-traffic-lights-visible"]
}
